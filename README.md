
# Pedalea

## Features

## Usage

```bash
# Builds swagger file
make docs
# Makes mocks for testint
make mocks
# Runs tests
make test
# Creates the db and runs all migrations
make db

  docker buildx build --platform linux/arm64 -t pedalea .
```
